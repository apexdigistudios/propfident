import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Prop Rules - Detailed Prop Firm Comparison | Propfident",
  description: "Compare detailed prop firm rules, drawdown limits, profit targets, and trading policies.",
};

export default function PropFirmsLayout({ children }: { children: React.ReactNode }) {
  return children;
}